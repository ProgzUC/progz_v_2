# Production build defaults (Vite embeds these at build time)
# Same-origin /api is rewritten by vercel.json → Render backend
VITE_API_BASE_URL=/api
VITE_COMPILER_BASE_URL=https://uccompiler.urbancode.in
